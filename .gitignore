# generated per-app backup scripts (regenerate from the wizard)
backup-*.sh
# editor / OS noise
*.swp
.DS_Store


# python bytecode
__pycache__/
*.pyc

# crash-test: regenerable noise (kept on disk, not in git)
crashtest-stack/appdata/cache/tempfile.bin

# crash-test runtime artifacts
crashtest-backups/
backup-crashtest.sh
.crashtest-last-run.log
.safety-*/
